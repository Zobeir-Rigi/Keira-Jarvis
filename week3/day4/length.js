function islongerThan (string, length) {
     if (string.length> length) {
        return true
    } else {
        return false
    }
    // return string.length> length
}
console.log(islongerThan(4,2))
